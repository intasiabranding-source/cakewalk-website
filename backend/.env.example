# Cakewalk Website Environment Variables

# Port configuration (Optional, default is 5000)
PORT=5000

# MongoDB Connection URI (Optional: Fallback to local JSON DB if not set)
# Replace with your actual MongoDB URI, e.g., mongodb+srv://username:password@cluster.mongodb.net/cakewalk
MONGODB_URI=mongodb+srv://santhosh:203santhosh40%cluster0.fmw6af9.mongodb.net/?appName=Cluster0

# Vercel Blob token (Optional: Fallback to local uploads/ directory if not set)
BLOB_READ_WRITE_TOKEN=
